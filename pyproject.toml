[project]
name = "helios"
version = "0.1"
requires-python = ">=3.14"
dependencies = [
	"jinja2==3.1.*",
	"werkzeug==3.1.*",
]

[project.scripts]
test = "helios.test:main"

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"
